{% extends 'base.html.twig' %}

{% block title %}MachinesGroups index{% endblock %}

{% block body %}
    <h1>MachinesGroups index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Description</th>
                <th>SerialNumber</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for machines_group in machines_groups %}
            <tr>
                <td>{{ machines_group.id }}</td>
                <td>{{ machines_group.name }}</td>
                <td>{{ machines_group.description }}</td>
                <td>{{ machines_group.serialNumber }}</td>
                <td>
                    <a href="{{ path('machines_groups_show', {'id': machines_group.id}) }}">show</a>
                    <a href="{{ path('machines_groups_edit', {'id': machines_group.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('machines_groups_new') }}">Create new</a>
{% endblock %}

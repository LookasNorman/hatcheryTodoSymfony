{% extends 'base.html.twig' %}

{% block title %}MachinesGroups{% endblock %}

{% block body %}
    <h1>MachinesGroups</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ machines_group.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ machines_group.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ machines_group.description }}</td>
            </tr>
            <tr>
                <th>SerialNumber</th>
                <td>{{ machines_group.serialNumber }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('machines_groups_index') }}">back to list</a>

    <a href="{{ path('machines_groups_edit', {'id': machines_group.id}) }}">edit</a>

    {{ include('machines_groups/_delete_form.html.twig') }}
{% endblock %}

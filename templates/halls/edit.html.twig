{% extends 'base.html.twig' %}

{% block title %}
    {% trans %}form.edit{% endtrans %} {% trans %}form.title.halls{% endtrans %}
{% endblock %}

{% block body %}
    <h1>
        {% trans %}form.edit{% endtrans %} {% trans %}form.title.halls{% endtrans %}
    </h1>

    {{ include('halls/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-outline-primary" href="{{ path('halls_index') }}">
        {% trans %}form.back{% endtrans %}
    </a>

    {{ include('halls/_delete_form.html.twig') }}
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}TodoType index{% endblock %}

{% block body %}
    <h1>TodoType index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for todo_type in todo_types %}
            <tr>
                <td>{{ todo_type.id }}</td>
                <td>{{ todo_type.name }}</td>
                <td>{{ todo_type.description }}</td>
                <td>
                    <a href="{{ path('todo_type_show', {'id': todo_type.id}) }}">show</a>
                    <a href="{{ path('todo_type_edit', {'id': todo_type.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('todo_type_new') }}">Create new</a>
{% endblock %}

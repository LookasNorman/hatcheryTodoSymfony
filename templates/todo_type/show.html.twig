{% extends 'base.html.twig' %}

{% block title %}TodoType{% endblock %}

{% block body %}
    <h1>TodoType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ todo_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ todo_type.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ todo_type.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('todo_type_index') }}">back to list</a>

    <a href="{{ path('todo_type_edit', {'id': todo_type.id}) }}">edit</a>

    {{ include('todo_type/_delete_form.html.twig') }}
{% endblock %}
